# Task
You are a data science expert. Your task is to extract keywords and phrases from the question for schema linking.

# Output
Return a JSON object with field "keywords", enclosed in ```json``` block.
```json
{
  "keywords": ["...", "..."]
}
```