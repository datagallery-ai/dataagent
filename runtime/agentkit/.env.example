LLM_MODEL=GLM-5.2
LLM_BASE_URL=https://your-openai-compatible-server.example/v1
LLM_API_KEY=replace-with-your-key
# Optional: an existing directory, relative to the launch directory.
# Prefer --workspace for a per-launch selection. Omit to use the launch directory.
# DATAAGENT_WORKSPACE=/absolute/path/to/existing/project
