AGENT_CONFIG:
  name: "NL2SQL Agent"
  backend: "langgraph"
  type: "nl2sql"
MODEL:
  chat_model:
    model_type: "chat"
    provider: "bailian"
    params:
      model: "qwen3-30b-a3b"
      temperature: 0.0
CORE:
  perceptor:
    user_sql_rules: "sql_rules_business_twin"
  generator:
    strategies: ["prompt"]
  validator:
    db_explain: false
    keyword_match: false
    metadata_match: false
  reflector:
    threshold: 0.9
DATABASE:
  db_id: "business_twin"
  dialect: "postgres"
  engine: "cloud_core"
  perceptor_type: "business_twin"
  config:
    path: ""
    explain_url: "http://"
SEMANTIC_LAYER:
  base_url: "http://"  # 可填服务根地址；客户端会统一到 /api/semantic/v1
  username: null                       # 未启用认证时保持 null；启用认证时填写
  password: null                       # 未启用认证时保持 null；启用认证时填写
  timeout: 30                           # 可按服务端表数量和响应耗时调整
  business_twin:
    table_selection:
      mode: "business_family"

# certificate:
#   # inbound_enabled / outbound_enabled 省略 = true
#   # outbound_ssl_services 省略 = [llm, semantic_layer]；[] = 出站全关
#   server_cert_file: /etc/dataagent/certs/server.crt
#   server_key_file: /etc/dataagent/certs/server.key
#   ca_cert_file: /etc/dataagent/certs/ca.crt
#   inbound_certificate_mode: 3
#   outbound_certificate_mode: 3
#   # inbound_cipher_suites: "ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384"
#   # outbound_cipher_suites: "ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384"
#   outbound_ca_cert_file: /etc/dataagent/certs/ca.crt
#   client_cert_file: /etc/dataagent/certs/dataagent-client.crt
#   client_key_file: /etc/dataagent/certs/dataagent-client.key
#   # outbound_ssl_services: [llm]
#   # inbound_enabled: false
#   # outbound_enabled: false
