## Question:
{{ question }}

## Candidate Context
The following content is the complete candidate set generated from the selected business id.

- `维度说明` explains the meaning of fields that may appear in candidate table families.
- `候选表簇` lists the only selectable table families and their actually available granularities.
- Select exactly one `family_name` and one `granularity` from this candidate context.

{{ tables }}
