# DataAgent 产品源

本目录为 GitCode `datagallery/dataagent` 的 `main` 完整快照，便于在 DataFoundry 仓库中作为 `product/` 一并演进。

- 来源：https://gitcode.com/datagallery/dataagent.git
- 分支：main
- 提交：87bb8bea106629dbf3ef03861d455d9592a818c0
- 导入说明：保留源仓库文件树，**不保留嵌套 `.git`**（由 DataFoundry 统一版本管理，直接在本目录改产品）。
- 忽略规则：`product/.gitignore` 继续生效；根目录 `.gitignore` 另有 `product/` 段，避免 `__pycache__`、`.venv`、`.env`、`output/`、`run_logs/`、`uv.lock` 等本地产物被提交。

